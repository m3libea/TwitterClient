<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- colorAccent is used as the default value for widgets -->
        <item name="colorAccent">@color/accent</item>
        <!-- Default color of the text in your views  -->
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="titleTextColor">@color/textMenus</item>
        <item name="android:colorBackground">@color/textMenus</item>
    </style>

    <style name="TwitterTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/primary</item>
        <item name="tabIndicatorHeight">2dp</item>
    </style>

</resources>
